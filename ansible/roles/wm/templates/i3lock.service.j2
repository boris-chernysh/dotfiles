[Unit]
Description=i3lock
Before=sleep.target

[Service]
User={{ username }}
Type=forking
Environment=DISPLAY=:0
ExecStart=/usr/bin/i3lock --ignore-empty-password --color=202020

[Install]
WantedBy=sleep.target
